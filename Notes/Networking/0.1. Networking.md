
# IP Addresses: 
An IP address is a unique numerical label assigned to every device on a network. From a cybersecurity standpoint, IP addresses are the primary way attackers locate, target, and track devices. Knowing your IP address topology is fundamental to understanding your attack surface.

**Key security notes:**
- IP addresses can be spoofed (IP spoofing) to forge the source of a packet
- Public IPs are exposed to the internet; private IPs sit behind NAT
- Attackers use tools like `nmap` to scan IP ranges and discover open services
- Logging IPs is essential for forensics and incident response

![[Pasted image 20260416110617.png]]

![[Pasted image 20260416111314.png|697]]

## IPv4 vs IPv6:

**IPv4** uses 32-bit addresses (e.g., `192.168.1.1`) — about 4.3 billion possible addresses. We've essentially run out, which is why NAT exists. **IPv6** uses 128-bit addresses (e.g., `2001:0db8::1`) — virtually unlimited.

Security implications of each:
![[Pasted image 20260422191027.png]]

|                   | IPv4                  | IPv6                                    |
| ----------------- | --------------------- | --------------------------------------- |
| Address space     | 4.2 billion           | 340 undecilion                          |
| Header complexity | simple                | More complex                            |
| NAT required?     | yes (hides devices)   | No(every device gets the public ip)     |
| Attack surface    | smaller per device    | Larger- every device directly reachable |
| weakness          | IP spoofing, scanning | IPv6 misconfiguration, rogue RAs        |

---
## OSI Layer:
![[Pasted image 20260416111927.png]]

**Key OSI security takeaway:** Firewalls primarily work at layers 3–4. WAFs (Web Application Firewalls) work at layer 7. IDS/IPS systems inspect across multiple layers. Most real-world attacks happen at layers 3, 4, and 7.

---
## MAC Address

A MAC (Media Access Control) address is a 48-bit hardware identifier burned into a network interface card (NIC). Format: `AA:BB:CC:DD:EE:FF`. The first 3 bytes identify the manufacturer (OUI), the last 3 are unique to the device.

**Security implications:**

- MAC addresses only travel within a local network segment — routers strip them
- MAC spoofing: attackers change their MAC to bypass MAC filtering on Wi-Fi or switches
- ARP poisoning: an attacker links their MAC to another device's IP, intercepting traffic (man-in-the-middle)
- MAC addresses appear in Wi-Fi probe requests — used to track people's physical location
- Modern OSes (iOS, Android, Windows 11) now randomize MACs for privacy

---

## DNS — The Phone Book Attackers Love

DNS (Domain Name System) translates human-readable names (`google.com`) into IP addresses (`142.250.80.46`). No DNS = no internet browsing.
![[Pasted image 20260416112235.png]]
**DNS security threats to know:**

- **DNS cache poisoning** — forged responses redirect users to malicious IPs
- **DNS tunneling** — attackers hide data exfiltration inside DNS queries (hard to detect)
- **DNS amplification DDoS** — small queries return huge responses, flooding a victim
- **Typosquatting** — registering `gooogle.com` to catch typos
- **DNS hijacking** — malware changes your router's DNS server to an attacker-controlled one.

---

## Common Ports and Protocols: 

![[Pasted image 20260626192114.png]]


## Sub-netting: 
- Subnetting is the process of dividing a large IP network into smaller logical networks called subnets.
- Each subnet allows devices to communicate efficiently, improving network performance, security, and manageability.
	- Each department is assigned a separate subnet
	- Devices within a department communicate using their own subnet
	- Inter-department traffic is routed through a router
	- Broadcast traffic is limited to each subnet
	- Network performance, security, and management are improved
- ![[Pasted image 20260627190714.png]]
- ![[Pasted image 20260628161024.png]]
